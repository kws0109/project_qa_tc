# 인수인계 — 다음 세션 시작점

> 이 파일은 대화 세션이 초기화된 뒤 작업을 이어가기 위한 문서입니다.
> 프로젝트 전반은 [README.md](README.md), 원래 계획은
> `<로컬 계획 문서>` 를 보세요.

**마지막 갱신**: 2026-08-13 · 코드 13,844줄 / 59파일 / 테스트 131개 통과

---

## 30초 요약

게임 QA 테스트케이스 자동 생성 도구(QATC)를 계획대로 전부 구현했고, **붕괴:스타레일에서
실제로 녹화·분석까지 성공**했습니다. 실측 과정에서 버그 6건을 잡아 고쳤습니다.

지금 남은 일은 **메뉴 위주 재녹화**입니다. 지난 녹화는 필드 이동이 대부분이라
TC로 쓸 값어치가 없었습니다.

---

## 바로 실행할 것

### 1. 관리자 권한 PowerShell 필수

게임이 High 무결성으로 돌아서 일반 권한으로는 **입력이 하나도 기록되지 않습니다**
(Windows UIPI). 일반 권한으로 실행하면 시작 전에 차단하고 이유를 알려줍니다.

시작 메뉴 → PowerShell **우클릭 → 관리자 권한으로 실행**:

```bash
cd <프로젝트 폴더>
.\.venv\Scripts\qatc.exe record --profile starrail
```

*(별칭 `qatc` 는 일반 세션 프로필이라 관리자 창에서는 전체 경로를 써야 합니다.)*

### 2. 무엇을 플레이할 것인가 — 이게 결과 품질을 좌우합니다

**메뉴·UI 동선만** 찍으세요. 필드에서 걷는 건 TC 대상이 아닙니다.

권장 동선 (3분이면 충분):

```
ESC 메뉴 → 캐릭터 → 유물 탭 → 부위 선택 → 장착 → ESC → ESC
ESC 메뉴 → 가방 → 재료 탭 → ESC
상단 아이콘 바에서 기원 / 우편 / 임무를 하나씩 들어갔다 나오기
```

- **각 화면에서 1~2초 머무를 것** (전이 완료 프레임 +1500ms가 잡혀야 함)
- **되돌아오기까지 할 것** (역방향 TC의 근거)
- 아이콘을 여러 개 눌러두면 나중에 아이콘 사전에 등록할 후보가 쌓임
- `F9` 북마크 · `F10` 일시정지 · `Ctrl+C` 종료

종료 시 `프레임 N장 · 입력 M건 · 규칙 제외 K건` 이 찍힙니다.
**M이 10 이상이면 정상**입니다.

### 3. 그다음

```bash
qatc analyze latest      # 화면·전이 추출
qatc name    latest      # LLM 명명 (API 키 필요)
qatc review  latest      # GUI 검토 + 아이콘 등록   ← 핵심
qatc tc      latest      # 테스트케이스 생성
qatc export  latest      # xlsx + 다이어그램
```

**API 키가 아직 등록되지 않았습니다.** `name`/`tc` 를 쓰려면:

```bash
qatc config --set-api-key
```

없어도 `record → analyze → review → export` 는 동작합니다 (화면 이름만 자동 이름).

---

## 실측으로 확인된 것

| 항목 | 결과 |
|---|---|
| 캡처 백엔드 | WGC / DXGI / GDI 3종 모두 스타레일에서 PASS |
| 커널 안티치트 | WGC 캡처는 `mhyprot2.sys` 환경에서 정상 동작 |
| 입력 훅 | **관리자 권한이면 동작**, 일반 권한이면 UIPI로 전면 차단 |
| OCR (RapidOCR 한국어) | 실제 게임에서 `아케론`·`어벤츄린`·`장낙천` 정확 인식 |
| Python 3.14 | 전 의존성 동작 (`paddlepaddle`만 불가 → RapidOCR로 대체) |

---

## 이번에 잡은 버그 6건 (전부 회귀 테스트 있음)

1. **WGC 캡처에 타이틀바 포함** — DWM 확장 프레임(1922×1112)을 클라이언트
   영역(1920×1080)으로 크롭하지 않아 세로 32px 어긋남. 클릭 좌표와 이미지
   좌표계가 달라져 UI 요소 매칭이 틀어졌습니다.
2. **키 자동 반복이 홀드를 연타로 기록** — `on_release` 미청취. W 키 1회 홀드가
   68회로 기록돼 40초 녹화에 프레임 627장·266MB가 나왔습니다.
3. **아이콘 12건 오매칭** — 아이콘 2개가 한 화면 요소 12개를 전부 자기 것이라
   주장. 자동 라벨 임계 0.90 + 상호 최선 매칭으로 해결.
4. **`combined_similarity` 가 텍스트를 묵살** — 아래 별도 항목 참고.
5. **`NormRect` 언팩 오류** — `static_ignore` 가 있는 실제 프로파일에서만 발생
   (픽스처는 비어 있어 숨어 있었음).
6. **진단 스크립트 세그폴트** — ctypes 반환 타입 미지정으로 64비트 포인터 절단.

---

## 설계가 바뀐 지점 (중요)

### `combined_similarity` — 텍스트 우선으로 확정

원래 "직접 정해달라"고 남겨뒀던 부분입니다. 실측 데이터로 결정했습니다.

스타레일 40초 세션에서 신호별 판별력을 측정한 결과:

| 신호 | 같은 화면 | 다른 화면 | 판별 간격 |
|---|---|---|---|
| **text** | 0.615 ~ 0.818 | **0.026** | **0.59** |
| cell | 0.431 ~ 0.792 | 0.318 | 0.11 (겹침) |
| layout | 0.223 ~ 0.723 | 0.072 | 0.15 |
| struct | 0.128 ~ 0.197 | 0.009 | 0.12 |

**HUD 텍스트가 화면 정체성의 가장 강한 증거**입니다. 3D 필드는 걸어다니면 배경이
통째로 바뀌어 셀 시그니처가 무너지지만 HUD는 그대로입니다.

옛 구현의 `if cell_sim < 0.60: 상한 0.50` 게이트가 텍스트를 묵살해
**같은 화면 7쌍 중 0쌍만 병합**됐고, 필드 화면 하나가 9개로 갈라졌습니다.
수정 후 **7/7 병합**, 필드↔메뉴는 0.079로 정확히 분리됩니다. 실제 세션이
13개 상태 → **7개**로 줄었습니다.

> ⚠️ `_calibrate_text()` 곡선은 **스타레일 한 세션에서 뽑은 값**입니다.
> 다른 게임(원신·명조·블루아카이브)을 붙일 때는 `compute_signals()` 값을
> 찍어 보고 재조정이 필요할 수 있습니다.

### 게임별 입력 규칙 (`profiles/*.yaml` 의 `input:`)

사용자 제보로 추가했습니다 — **스타레일 메인화면(필드)에서는 Alt를 홀드해야
마우스 포인터가 활성화되고, 메뉴에 들어가면 홀드 없이 활성화됩니다.**

```yaml
input:
  pointer_modifier: alt              # 이 키 없이 한 클릭은 좌표가 (0.50,0.50)에 고정 → 신뢰 불가
  ignore_keys: [w, a, s, d, space, ctrl, shift, tab, q, f, v]
  drop_unmodified_clicks: false      # 클릭 사실은 남기되 좌표만 불확실 표시
```

실측 세션 159건 입력 중 **147건이 이 규칙으로 걸러졌습니다** (이동 키 + Alt 홀드).

필터는 **녹화 시점과 분석 시점 양쪽**에 있습니다. 녹화 필터는 디스크를 아끼고,
분석 필터는 프로파일을 고친 뒤 **재녹화 없이 재분석**할 수 있게 합니다.

---

## 알아둘 한계

- **필드(오픈월드)는 TC 대상이 아닙니다.** 3D 배경이 계속 바뀌어 화면 식별이
  어렵고, 걸어다닌 기록에서는 쓸만한 TC가 안 나옵니다. 메뉴·UI 플로우용입니다.
- **전투 화면도 마찬가지**입니다 — 화면 전체가 이펙트라 안정 셀이 거의 없습니다.
- OCR 정확도는 게임 폰트 특성상 완벽하지 않습니다. 그래서 **보조 신호**로만
  쓰도록 설계했는데, 실측 결과 오히려 텍스트가 가장 판별력이 높았습니다
  (HUD가 안정적이기 때문). 자카드 비교라 몇 글자 틀려도 결과가 안 뒤집힙니다.

---

## 아직 안 해본 것

- [ ] **메뉴 위주 재녹화** ← 다음에 할 일
- [ ] LLM 화면 명명 (`qatc name`) — API 키 미등록이라 한 번도 안 돌려봄
- [ ] TC 생성 (`qatc tc`) — 실제 게임 데이터로 미검증 (합성 픽스처로만 확인)
- [ ] 아이콘 사전 실전 등록 — 구현·테스트는 끝났으나 실제 게임 아이콘으로는 미등록
- [ ] 리뷰 GUI 실사용 — 합성 데이터로만 확인
- [ ] 원신·명조·블루아카이브 프로파일 실측 검증

---

## 세션 상태

```
sessions/
  20260813-224129_starrail   실제 녹화 (40초, 필드 위주) — 분석 완료, 상태 7개/전이 11개
  fixture_synthetic          합성 픽스처 (파이프라인 검증용, 정답: 화면 4개/전이 6개)
```

`scripts/make_fixture.py` 로 픽스처를 언제든 재생성할 수 있습니다.

## 진단 도구

```bash
python scripts/spike.py --profile starrail          # 캡처·OCR·훅·변동성 4항목 검사
python scripts/diag_input.py --profile starrail     # 입력이 어디서 사라지는지 추적
```

`diag_input.py` 는 훅이 못 받은 것(권한 문제)과 레코더가 버린 것(코드 버그)을
구분해 줍니다. 입력이 기록되지 않으면 이것부터 돌리세요.
