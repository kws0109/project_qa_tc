# QATC 스탠드얼론 앱 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 인터뷰·검토·산출을 한 창에서 한다 — 왼쪽 트리, 가운데 채팅, 오른쪽 TC 검토.

**Architecture:** 로컬 Flask 백엔드 + 브라우저 단일 페이지. **백엔드는 지식 DB 를 읽기만 한다.** 모든 쓰기는 `claude` 자식 프로세스가 `qatc` CLI 를 부르는 경로로만 일어나므로 게이트를 우회할 수 없다. 대화는 로컬 `claude -p`(OAuth=구독)로 처리해 API 비용 제약을 유지한다.

**Tech Stack:** Python 3.11+ · Flask · stdlib `subprocess` · `openpyxl` · `pytest`

**설계:** [../specs/2026-08-14-qatc-app-design.md](../specs/2026-08-14-qatc-app-design.md)

## Global Constraints

- Windows 전용. 경로는 `pathlib.Path` 로만 다룬다.
- 콘솔 출력은 `qatc/console.py` 의 `_p()`. 진단은 `_p(msg, err=True)` 로 stderr. 맨 `print()` 금지.
- 테스트 실행: `.venv\Scripts\python.exe -m pytest` — `-q` 를 더 붙이면 `-qq` 가 되어 개수 줄이 사라진다.
- **백엔드는 지식 DB 에 쓰지 않는다.** `add_testcase` · `set_slot` · `init_content` · `replace_generated` 를 호출하는 코드가 `qatc/app/` 안에 있으면 안 된다.
- 사용자·화면에 보이는 문자열은 한국어.
- 오류는 다음 조치를 항상 함께 알린다. 파이썬 예외 이름을 그대로 노출하지 않는다.
- `--json` 계열 출력의 stdout 은 순수 JSON 이어야 한다 (기존 규칙).
- 진짜 Anthropic API 를 호출하지 않는다. 테스트는 `claude` 를 가짜 실행 파일로 바꿔치기한다.
- 커밋 메시지는 한국어. 마지막 줄에 `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`.
- 시작 시점 테스트 수: **422 passed**.
- **새 테스트는 전부 뮤테이션으로 검증한다** — 구현을 깨뜨려 그 테스트가 실패하는지 확인하고 원문을 **에디터로 다시 써서** 복원한다. `git checkout`/`stash`/`reset` 금지. 빈 문자열로 치환하는 뮤테이션 금지.

---

## File Structure

| 파일 | 변경 | 책임 | 모르는 것 |
|---|---|---|---|
| `qatc/knowledge/store.py` | 수정 | `testcase_meta()` 추가 | — |
| `qatc/app/__init__.py` | 신규 | 패키지 | — |
| `qatc/app/views.py` | 신규 | SQLite → JSON | HTTP, `claude` |
| `qatc/app/chat.py` | 신규 | `claude` 자식 프로세스·세션·스트림 파싱 | SQLite, HTTP |
| `qatc/app/server.py` | 신규 | 라우팅 · SSE · 정적 파일 | SQLite 스키마 |
| `qatc/app/static/index.html` | 신규 | 3분할 레이아웃 | — |
| `qatc/app/static/app.css` | 신규 | 스타일 | — |
| `qatc/app/static/app.js` | 신규 | fetch · SSE 수신 · 렌더 | — |
| `qatc/cli.py` | 수정 | `qatc app` 명령 | — |
| `pyproject.toml` | 수정 | `flask` 의존성 | — |
| `tests/test_app_views.py` | 신규 | JSON 변환 | |
| `tests/test_app_chat.py` | 신규 | 가짜 `claude` 로 스트림·401·세션 | |
| `tests/test_app_server.py` | 신규 | 라우트·SSE·오류 | |

`views.py` 는 `claude` 를 모르고 `chat.py` 는 SQLite 를 모른다. 둘 다 독립적으로 테스트된다.

## ★ 착수 전에 알아야 할 것

**`slot_keys` 는 지금 쓰기 전용이다.** `add_testcase(content, family, tc, slot_keys)` 가 저장하지만 `testcases()` 는 `TestCase` 만 돌려주고 그 열을 읽지 않는다. 선행 브랜치 리뷰가 "읽는 코드가 하나도 없다"고 지적했고 그때는 이월했다. **이 앱이 첫 소비자다** — Task 1 이 `testcase_meta()` 로 `generated_hash` 와 함께 한 쿼리에 읽는다.

**`claude` 토큰이 만료돼 있을 수 있다.** 설계 시점 실측에서 `claude -p` 가 `401 OAuth access token has expired` 를 냈다. 메커니즘(OAuth=구독)은 확인됐지만 성공 호출은 미확인이다. **Task 3 착수 전에 아래를 실행해 확인할 것:**

```
claude -p "Reply with exactly: OK" --output-format json --model sonnet
```

`is_error: true` 가 나오면 터미널에서 `claude` 를 띄워 로그인한 뒤 진행한다. **테스트는 진짜 `claude` 를 부르지 않으므로** 이 확인이 실패해도 Task 3 자체는 구현·테스트할 수 있다 — 다만 Task 4 의 수동 확인이 불가능하니 보고할 것.

---

### Task 1: `slot_keys` 읽기 + 뷰 계층

**Files:**
- Modify: `qatc/knowledge/store.py`
- Create: `qatc/app/__init__.py`, `qatc/app/views.py`
- Create: `tests/test_app_views.py`

**Interfaces:**
- Consumes: `KnowledgeStore`, `plan_families`, `withdrawn_families`, `testcase_hash`, `SlotStatus`, `AppConfig`
- Produces:
  - `KnowledgeStore.testcase_meta(content: str) -> dict[str, tuple[list[str], str]]` — TC id → (`slot_keys`, `generated_hash`). **컨텐츠당 쿼리 한 번.**
  - `qatc.app.views.tree(cfg: AppConfig) -> dict` — `/api/tree` 의 본문
  - `qatc.app.views.content_detail(cfg: AppConfig, game: str, name: str) -> dict` — `/api/content` 의 본문
  - `qatc.app.views.ContentNotFound(Exception)` — 메시지는 완성된 한국어 문장

**읽기 전용 규칙.** `views.py` 는 `KnowledgeStore` 의 읽기 메서드만 부른다 (`list_contents`, `slots`, `testcases`, `get_content`, `testcase_meta`, `game`). 쓰기 메서드를 부르면 Task 3 의 감시 테스트(`test_the_backend_never_writes_to_the_knowledge_db`)가 실패한다.

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_app_views.py`:

```python
"""SQLite → JSON. 앱의 왼쪽·오른쪽 패널이 읽는 모양."""

import json

import pytest

from qatc.app.views import ContentNotFound, content_detail, tree
from qatc.config import AppConfig
from qatc.knowledge.models import SlotStatus
from qatc.knowledge.store import KnowledgeStore
from qatc.models import Priority, TCKind, TCOrigin, TestCase


@pytest.fixture()
def cfg(tmp_path):
    """스크래치 지식 루트만 보는 설정."""
    return AppConfig(knowledge_root=str(tmp_path / "k"),
                     profiles_dir=str(tmp_path / "p"))


def _tc(title="TC", family="정상 경로"):
    return TestCase(id="", category_minor=family, title=title,
                    steps=["1"], expected=["e"],
                    priority=Priority.HIGH, kind=TCKind.HAPPY_PATH,
                    origin=TCOrigin.INTERVIEW)


def _seed(cfg, game="starrail", name="파티편성"):
    with KnowledgeStore(cfg.knowledge_path / f"{game}.db") as st:
        st.init_content(name, game=game, types=["편성"])
        st.set_slot(name, "core_action", SlotStatus.FILLED, "파티를 편성한다")
        st.set_slot(name, "cost", SlotStatus.UNKNOWN)
        st.add_testcase(name, "정상 경로", _tc(), ["core_action"])
    return name


def test_tree_lists_games_and_contents(cfg):
    _seed(cfg)
    t = tree(cfg)
    assert [g["game"] for g in t["games"]] == ["starrail"]
    c = t["games"][0]["contents"][0]
    assert c["name"] == "파티편성"
    assert c["types"] == ["편성"]


def test_tree_filled_counts_only_filled_slots(cfg):
    """UNKNOWN·NA 는 근거가 아니므로 진척에도 안 들어간다.

    이 구분이 게이트에서는 봉인돼 있는데 사용자가 보는 진척 표시에서
    무너지면, 절반이 '모름' 인 컨텐츠가 완료로 보인다.
    """
    _seed(cfg)   # FILLED 1개 + UNKNOWN 1개
    c = tree(cfg)["games"][0]["contents"][0]
    assert c["filled"] == 1
    assert c["total"] == 14


def test_tree_marks_planned_and_blocked_families(cfg):
    _seed(cfg)
    fams = {f["family"]: f for f in tree(cfg)["games"][0]["contents"][0]["families"]}
    assert fams["정상 경로"]["planned"] is True
    assert fams["정상 경로"]["tc_count"] == 1
    assert fams["재화 부족"]["planned"] is False
    assert fams["재화 부족"]["blocked_by"] == "cost"
    assert "모른다" in fams["재화 부족"]["reason"]


def test_tree_marks_withdrawn_evidence(cfg):
    """근거를 철회해도 TC 는 남는다 — 트리가 그것을 표시해야 한다."""
    name = _seed(cfg)
    with KnowledgeStore(cfg.knowledge_path / "starrail.db") as st:
        st.set_slot(name, "core_action", SlotStatus.NA)
    fams = {f["family"]: f for f in tree(cfg)["games"][0]["contents"][0]["families"]}
    assert fams["정상 경로"]["withdrawn"] is True
    assert fams["정상 경로"]["tc_count"] == 1     # 지우지 않는다


def test_tree_db_mtime_is_per_game(cfg):
    _seed(cfg)
    assert set(tree(cfg)["db_mtime"]) == {"starrail"}


def test_tree_on_empty_root_is_not_an_error(cfg):
    t = tree(cfg)
    assert t["games"] == []


def test_content_detail_carries_slots_and_testcases(cfg):
    name = _seed(cfg)
    d = content_detail(cfg, "starrail", name)
    keys = {s["key"]: s for s in d["slots"]}
    assert keys["core_action"]["status"] == "filled"
    assert keys["core_action"]["value"] == "파티를 편성한다"
    assert len(d["testcases"]) == 1
    assert d["testcases"][0]["family"] == "정상 경로"


def test_content_detail_exposes_slot_keys(cfg):
    """`slot_keys` 는 지금까지 쓰기 전용이었다 — 이 앱이 첫 소비자다."""
    name = _seed(cfg)
    assert content_detail(cfg, "starrail", name)["testcases"][0]["slot_keys"] == ["core_action"]


def test_content_detail_flags_a_human_edited_testcase(cfg):
    """`generated_hash` 와 본문 해시가 다르면 사람이 손댄 것."""
    name = _seed(cfg)
    with KnowledgeStore(cfg.knowledge_path / "starrail.db") as st:
        tc = st.testcases(name)[0]
        tc.title = "사람이 고친 제목"
        st.update_testcase_row(tc)
    d = content_detail(cfg, "starrail", name)
    assert d["testcases"][0]["edited"] is True


def test_content_detail_does_not_flag_an_untouched_testcase(cfg):
    name = _seed(cfg)
    assert content_detail(cfg, "starrail", name)["testcases"][0]["edited"] is False


def test_content_detail_marks_withdrawn_testcases(cfg):
    name = _seed(cfg)
    with KnowledgeStore(cfg.knowledge_path / "starrail.db") as st:
        st.set_slot(name, "core_action", SlotStatus.NA)
    assert content_detail(cfg, "starrail", name)["testcases"][0]["withdrawn"] is True


def test_content_detail_on_missing_content_says_what_to_do(cfg):
    _seed(cfg)
    with pytest.raises(ContentNotFound) as e:
        content_detail(cfg, "starrail", "없는것")
    msg = str(e.value)
    assert "없는것" in msg
    assert "slot init" in msg          # 다음 조치
    assert "ContentNotFound" not in msg  # 예외 이름을 노출하지 않는다


def test_everything_is_json_serialisable(cfg):
    """Flask 가 직렬화할 수 있어야 한다 — Enum·Path 가 새면 여기서 죽는다."""
    name = _seed(cfg)
    json.dumps(tree(cfg), ensure_ascii=False)
    json.dumps(content_detail(cfg, "starrail", name), ensure_ascii=False)
```

- [ ] **Step 2: 실패 확인**

Run: `.venv\Scripts\python.exe -m pytest tests/test_app_views.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'qatc.app'`

- [ ] **Step 3: 메타 열 읽기 메서드 추가**

`qatc/knowledge/store.py` 의 `testcases()` 아래에 넣는다. `replace_generated` 가 이미 같은 두 열을 한 쿼리로 읽으므로 그 관용구를 따른다:

```python
    def testcase_meta(self, content: str) -> dict[str, tuple[list[str], str]]:
        """이 컨텐츠 TC 들의 `(slot_keys, generated_hash)` 를 id 로 찾는 표.

        두 열을 **한 쿼리로** 가져온다. 앱의 오른쪽 패널이 TC 마다
        "왜 존재하는가"(근거 슬롯)와 "사람이 고쳤는가"(해시 비교)를 둘 다
        보여주는데, TC 당 DB 를 다시 여는 구현은 받아들이지 않는다.

        `slot_keys` 는 `add_testcase` 가 저장해 온 열인데 지금껏 읽는 코드가
        없었다 — 선행 리뷰가 "테스트 전용 아니냐"고 지적한 자리이고,
        이 앱이 첫 소비자다.
        """
        rows = self._db().execute(
            "SELECT id, slot_keys, generated_hash FROM testcases WHERE content = ?",
            (content,),
        ).fetchall()
        return {r["id"]: (json.loads(r["slot_keys"]), r["generated_hash"]) for r in rows}
```

- [ ] **Step 4: `qatc/app/__init__.py` 작성**

```python
"""3분할 로컬 웹앱 — 왼쪽 트리 · 가운데 채팅 · 오른쪽 TC 검토.

**이 패키지는 지식 DB 에 쓰지 않는다.** 슬롯과 TC 의 모든 변경은 채팅이
`claude` 를 거쳐 `qatc` CLI 를 부르는 경로로만 일어난다. 그래야 "근거 없는
TC는 만들어지지 않는다" 를 강제하는 게이트를 앱이 우회할 수 없다.
"""
```

- [ ] **Step 5: `qatc/app/views.py` 구현**

```python
"""SQLite → JSON. 읽기만 한다."""

from __future__ import annotations

from pathlib import Path

from ..config import AppConfig
from ..knowledge.gate import plan_families, withdrawn_families
from ..knowledge.models import SlotStatus
from ..knowledge.store import KnowledgeStore, testcase_hash


class ContentNotFound(Exception):
    """그 컨텐츠가 없다. 메시지는 완성된 한국어 문장이다."""


def _db_paths(cfg: AppConfig) -> list[Path]:
    return sorted(cfg.knowledge_path.glob("*.db"))


def tree(cfg: AppConfig) -> dict:
    """왼쪽 패널 전체."""
    games, mtimes = [], {}
    for p in _db_paths(cfg):
        mtimes[p.stem] = p.stat().st_mtime
        contents = []
        with KnowledgeStore(p) as st:
            for c in st.list_contents():
                slots = st.slots(c.name)
                cases = st.testcases(c.name)
                planned, skipped = plan_families(slots)
                withdrawn = withdrawn_families(slots, {t.category_minor for t in cases})
                counts: dict[str, int] = {}
                for t in cases:
                    counts[t.category_minor] = counts.get(t.category_minor, 0) + 1
                fams = [
                    {"family": fp.family, "planned": True,
                     "tc_count": counts.get(fp.family, 0),
                     "withdrawn": fp.family in withdrawn}
                    for fp in planned
                ] + [
                    {"family": fs.family, "planned": False,
                     "tc_count": counts.get(fs.family, 0),
                     "withdrawn": fs.family in withdrawn,
                     "blocked_by": fs.slot_key, "reason": fs.reason}
                    for fs in skipped
                ]
                contents.append({
                    "name": c.name,
                    "types": list(c.types),
                    "filled": sum(1 for s in slots if s.status is SlotStatus.FILLED),
                    "total": len(slots),
                    "families": fams,
                })
        games.append({"game": p.stem, "contents": contents})
    return {"db_mtime": mtimes, "games": games}


def content_detail(cfg: AppConfig, game: str, name: str) -> dict:
    """가운데·오른쪽이 함께 쓰는 상세."""
    path = cfg.knowledge_path / f"{game}.db"
    if not path.exists():
        raise ContentNotFound(
            f"'{game}' 지식 DB가 없습니다. "
            f"채팅에서 'qatc slot init <컨텐츠> --game {game}' 으로 먼저 만드세요."
        )
    with KnowledgeStore(path) as st:
        c = st.get_content(name)
        if c is None:
            raise ContentNotFound(
                f"컨텐츠 '{name}'가 없습니다. "
                f"채팅에서 'qatc slot init {name}' 으로 먼저 만드세요."
            )
        slots = st.slots(name)
        cases = st.testcases(name)
        meta = st.testcase_meta(name)          # id -> (slot_keys, generated_hash)
    withdrawn = withdrawn_families(slots, {t.category_minor for t in cases})
    return {
        "name": c.name, "game": game, "types": list(c.types),
        "slots": [
            {"key": s.key, "hint": s.prompt_hint, "family": s.tc_family,
             "status": s.status.value, "value": s.value}
            for s in slots
        ],
        "testcases": [
            {"id": t.id, "family": t.category_minor, "title": t.title,
             "kind": t.kind.value, "priority": t.priority.value,
             "origin": t.origin.value, "precondition": t.precondition,
             "steps": list(t.steps), "expected": list(t.expected),
             "rationale": t.rationale, "slot_keys": meta[t.id][0],
             "edited": testcase_hash(t) != meta[t.id][1],
             "withdrawn": t.category_minor in withdrawn}
            for t in cases
        ],
    }
```

`edited` 판정은 `replace_generated` 가 쓰는 것과 **같은 식**이다 (`testcase_hash(tc) != r["generated_hash"]`). 두 곳이 갈리면 재생성이 보존하는 TC 와 화면이 "사람이 고침" 이라 부르는 TC 가 달라진다.

- [ ] **Step 6: 통과 확인**

Run: `.venv\Scripts\python.exe -m pytest`
Expected: 422 + 13 = 435 passed

- [ ] **Step 7: 뮤테이션 검증**

| # | 뮤테이션 | 죽어야 하는 테스트 |
|---|---|---|
| M1 | `filled` 를 `s.is_closed` 로 | `test_tree_filled_counts_only_filled_slots` |
| M2 | `withdrawn` 을 항상 `False` 로 | `test_tree_marks_withdrawn_evidence`, `test_content_detail_marks_withdrawn_testcases` |
| M3 | `edited` 를 항상 `False` 로 | `test_content_detail_flags_a_human_edited_testcase` |
| M4 | `edited` 를 항상 `True` 로 | `test_content_detail_does_not_flag_an_untouched_testcase` |
| M5 | `testcase_meta` 가 `slot_keys` 를 `[]` 로 반환 | `test_content_detail_exposes_slot_keys` |
| M6 | `ContentNotFound` 메시지에서 `slot init` 제거 | `test_content_detail_on_missing_content_says_what_to_do` |
| M7 | `skipped` 계열을 `families` 에서 누락 | `test_tree_marks_planned_and_blocked_families` |
| M8 | `status` 를 `s.status` (Enum) 그대로 내보내기 | `test_everything_is_json_serialisable` |

- [ ] **Step 8: 커밋**

```bash
git add qatc/knowledge/store.py qatc/app tests/test_app_views.py
git commit -m "앱 뷰 계층 — SQLite 를 읽어 JSON 으로 (slot_keys 첫 소비자)"
```

---

### Task 2: `claude` 자식 프로세스

**Files:**
- Create: `qatc/app/chat.py`
- Create: `tests/test_app_chat.py`

**Interfaces:**
- Produces:
  - `ChatEvent` — `kind: str` (`delta`/`tool`/`done`/`error`), `data: dict`
  - `stream_turn(cfg, message: str, content: str | None, *, claude: str | None = None) -> Iterator[ChatEvent]`
  - `session_id_for(cfg, content: str | None) -> str` — 컨텐츠별 UUID, `knowledge_root/sessions.json` 에 영속
  - `ClaudeMissing(Exception)` — 실행 파일을 못 찾음. 메시지는 완성된 한국어 문장

**진짜 `claude` 를 부르지 않는다.** 테스트는 정해진 stream-json 을 표준출력으로 뱉는 가짜 실행 파일을 만들어 `claude=` 로 주입한다.

**실행 계약:**

```
claude -p
  --session-id <uuid>
  --output-format stream-json
  --input-format stream-json
  --append-system-prompt <SKILL.md 전문>
  --model opus
  cwd = 프로젝트 루트
```

`cwd` 가 프로젝트 루트여야 `.claude/settings.json` 의 Bash allowlist 가 적용된다. 헤드리스에는 권한 승인 창에 답할 UI 가 없으므로 필수 조건이다.

- [ ] **Step 1: 가짜 `claude` 픽스처와 실패하는 테스트 작성**

`tests/test_app_chat.py`:

```python
"""`claude` 자식 프로세스. 진짜 API 는 부르지 않는다."""

import json
import sys
import textwrap

import pytest

from qatc.app.chat import ChatEvent, ClaudeMissing, session_id_for, stream_turn
from qatc.config import AppConfig


@pytest.fixture()
def cfg(tmp_path):
    return AppConfig(knowledge_root=str(tmp_path / "k"),
                     profiles_dir=str(tmp_path / "p"))


def _fake_claude(tmp_path, lines):
    """정해진 stream-json 줄을 뱉는 가짜 실행 파일을 만든다."""
    script = tmp_path / "fake_claude.py"
    script.write_text(textwrap.dedent(f"""
        import sys
        for line in {lines!r}:
            sys.stdout.write(line + "\\n")
            sys.stdout.flush()
    """), encoding="utf-8")
    return [sys.executable, str(script)]


OK_LINES = [
    json.dumps({"type": "assistant", "message": {"content": [
        {"type": "text", "text": "안녕"}]}}, ensure_ascii=False),
    json.dumps({"type": "assistant", "message": {"content": [
        {"type": "tool_use", "name": "Bash",
         "input": {"command": "qatc tc add 파티편성 ..."}}]}}, ensure_ascii=False),
    json.dumps({"type": "result", "subtype": "success", "is_error": False},
               ensure_ascii=False),
]

AUTH_LINES = [
    json.dumps({"type": "result", "subtype": "success", "is_error": True,
                "api_error_status": 401,
                "result": "Failed to authenticate. API Error: 401 OAuth access token has expired."},
               ensure_ascii=False),
]


def test_text_arrives_as_delta_events(cfg, tmp_path):
    evs = list(stream_turn(cfg, "안녕", None, claude=_fake_claude(tmp_path, OK_LINES)))
    deltas = [e for e in evs if e.kind == "delta"]
    assert "".join(d.data["text"] for d in deltas) == "안녕"


def test_tool_use_becomes_a_tool_event(cfg, tmp_path):
    evs = list(stream_turn(cfg, "x", None, claude=_fake_claude(tmp_path, OK_LINES)))
    tools = [e for e in evs if e.kind == "tool"]
    assert len(tools) == 1
    assert tools[0].data["name"] == "Bash"
    assert "qatc tc add" in tools[0].data["summary"]


def test_a_turn_ends_with_exactly_one_done(cfg, tmp_path):
    evs = list(stream_turn(cfg, "x", None, claude=_fake_claude(tmp_path, OK_LINES)))
    assert [e.kind for e in evs].count("done") == 1
    assert evs[-1].kind == "done"


def test_expired_token_becomes_an_auth_error_not_a_done(cfg, tmp_path):
    """401 은 유일하게 *성공한 것처럼 보이는* 실패라 1순위다.

    조용히 지나가면 사용자는 앱이 고장난 줄 안다.
    """
    evs = list(stream_turn(cfg, "x", None, claude=_fake_claude(tmp_path, AUTH_LINES)))
    errs = [e for e in evs if e.kind == "error"]
    assert len(errs) == 1
    assert errs[0].data["kind"] == "auth"
    assert "재인증" in errs[0].data["message"]
    assert "claude" in errs[0].data["message"]      # 무엇을 실행할지
    assert not [e for e in evs if e.kind == "done"]  # 성공으로 끝내지 않는다


def test_unparseable_line_does_not_kill_the_turn(cfg, tmp_path):
    """stream-json 이 아닌 줄이 섞여도 나머지를 계속 읽는다."""
    lines = ["이건 JSON 이 아니다"] + OK_LINES
    evs = list(stream_turn(cfg, "x", None, claude=_fake_claude(tmp_path, lines)))
    assert evs[-1].kind == "done"


def test_missing_executable_says_what_to_do(cfg):
    with pytest.raises(ClaudeMissing) as e:
        list(stream_turn(cfg, "x", None, claude=["존재하지않는실행파일"]))
    msg = str(e.value)
    assert "claude" in msg
    assert "설치" in msg or "찾을 수 없" in msg
    assert "FileNotFoundError" not in msg


def test_session_id_is_stable_per_content(cfg):
    a = session_id_for(cfg, "파티편성")
    assert session_id_for(cfg, "파티편성") == a
    assert session_id_for(cfg, "상점") != a


def test_session_id_survives_a_restart(cfg):
    a = session_id_for(cfg, "파티편성")
    assert (cfg.knowledge_path / "sessions.json").exists()
    assert session_id_for(cfg, "파티편성") == a


def test_no_content_uses_the_default_session(cfg):
    """컨텐츠를 아직 안 고른 대화도 이어져야 한다."""
    assert session_id_for(cfg, None) == session_id_for(cfg, None)
    assert session_id_for(cfg, None) != session_id_for(cfg, "파티편성")
```

- [ ] **Step 2: 실패 확인**

Run: `.venv\Scripts\python.exe -m pytest tests/test_app_chat.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'qatc.app.chat'`

- [ ] **Step 3: 구현**

`qatc/app/chat.py` 를 위 인터페이스대로 쓴다. 요점만:

- `subprocess.Popen(..., stdout=PIPE, stderr=PIPE, text=True, encoding="utf-8", cwd=project_root())` 로 띄우고 stdout 을 한 줄씩 읽으며 `ChatEvent` 로 변환한다.
- **`json.JSONDecodeError` 는 그 줄만 건너뛴다.** 한 줄이 깨졌다고 턴 전체를 버리면 안 된다.
- `type == "result"` 에서 `is_error` 이고 `api_error_status == 401` 이면 `error(kind="auth")` 를 내고 **`done` 을 내지 않는다.**
- `--append-system-prompt` 에는 `.claude/skills/interview/SKILL.md` 전문을 읽어 넣는다.
- `FileNotFoundError` 는 `ClaudeMissing` 으로 바꿔 던진다.
- 세션 id 는 `knowledge_root/sessions.json` 의 `{컨텐츠이름 또는 "__default__": uuid}` 에 저장한다. `uuid.uuid4()` 를 쓴다.

- [ ] **Step 4: 통과 확인**

Run: `.venv\Scripts\python.exe -m pytest`
Expected: 435 + 9 = 444 passed

- [ ] **Step 5: 뮤테이션 검증**

| # | 뮤테이션 | 죽어야 하는 테스트 |
|---|---|---|
| M9 | 401 분기를 지우고 `done` 을 내게 | `test_expired_token_becomes_an_auth_error_not_a_done` |
| M10 | `api_error_status` 검사 없이 `is_error` 만 보기 | 위와 동일 (다른 상태 코드가 auth 로 둔갑) |
| M11 | `JSONDecodeError` 에서 턴을 중단 | `test_unparseable_line_does_not_kill_the_turn` |
| M12 | `tool_use` 를 무시 | `test_tool_use_becomes_a_tool_event` |
| M13 | 세션 id 를 매번 새로 생성 | `test_session_id_is_stable_per_content`, `test_session_id_survives_a_restart` |
| M14 | `None` 컨텐츠를 `"파티편성"` 과 같은 키로 | `test_no_content_uses_the_default_session` |
| M15 | `ClaudeMissing` 대신 `FileNotFoundError` 전파 | `test_missing_executable_says_what_to_do` |
| M16 | `done` 을 두 번 내기 | `test_a_turn_ends_with_exactly_one_done` |

- [ ] **Step 6: 커밋**

```bash
git add qatc/app/chat.py tests/test_app_chat.py
git commit -m "채팅 계층 — claude 자식 프로세스와 스트림 파싱"
```

---

### Task 3: 서버와 라우트

**Files:**
- Create: `qatc/app/server.py`
- Create: `tests/test_app_server.py`
- Modify: `pyproject.toml` (`flask` 추가)

**Interfaces:**
- Produces: `create_app(cfg: AppConfig) -> Flask`, `run(cfg, port: int = 8765) -> None`

**라우트:** `GET /`, `GET /api/tree`, `GET /api/content`, `GET /api/health`, `POST /api/chat` (SSE), `POST /api/export`.

- [ ] **Step 1: `flask` 를 의존성에 추가**

`pyproject.toml` 의 `dependencies` 에 `"flask>=3.0",` 를 넣고 설치한다:

```bash
.venv\Scripts\python.exe -m pip install -e .
```

**왜 stdlib 이 아닌가** — 라우팅·SSE·동시 요청을 직접 짜면 새로 만드는 코드가 늘고, "안정성은 새로 만들지 않은 코드에서 나온다"는 이 결정의 기준에 어긋난다. 근거를 `pyproject.toml` 주석에 한 줄 남겨라 — 선행 브랜치에서 `pyyaml` 이 "왜 있는지 문서에 없는 의존성" 상태였고 리뷰가 그것을 지적했다.

- [ ] **Step 2: 실패하는 테스트 작성**

`tests/test_app_server.py`:

```python
"""라우트·SSE·오류. Flask test client 로만 돈다."""

import json

import pytest

from qatc.app.server import create_app
from qatc.config import AppConfig
from qatc.knowledge.models import SlotStatus
from qatc.knowledge.store import KnowledgeStore


@pytest.fixture()
def app(tmp_path):
    cfg = AppConfig(knowledge_root=str(tmp_path / "k"),
                    profiles_dir=str(tmp_path / "p"))
    with KnowledgeStore(cfg.knowledge_path / "starrail.db") as st:
        st.init_content("파티편성", game="starrail", types=["편성"])
        st.set_slot("파티편성", "core_action", SlotStatus.FILLED, "편성한다")
    a = create_app(cfg)
    a.config["TESTING"] = True
    return a


def test_index_is_served(app):
    r = app.test_client().get("/")
    assert r.status_code == 200
    assert b"<html" in r.data.lower()


def test_tree_returns_json(app):
    r = app.test_client().get("/api/tree")
    assert r.status_code == 200
    assert r.get_json()["games"][0]["game"] == "starrail"


def test_content_returns_json(app):
    r = app.test_client().get("/api/content?game=starrail&name=파티편성")
    assert r.status_code == 200
    assert r.get_json()["name"] == "파티편성"


def test_missing_content_is_a_korean_error_not_a_traceback(app):
    r = app.test_client().get("/api/content?game=starrail&name=없는것")
    assert r.status_code == 404
    body = r.get_json()
    assert "없는것" in body["error"]
    assert "ContentNotFound" not in body["error"]
    assert "Traceback" not in body["error"]


def test_health_reports_claude_state(app):
    body = app.test_client().get("/api/health").get_json()
    assert body["claude"] in {"ok", "missing", "unauthenticated"}
    assert "knowledge_root" in body


def test_chat_streams_server_sent_events(app, monkeypatch):
    from qatc.app import chat as chat_mod

    def fake_stream(cfg, message, content, **kw):
        yield chat_mod.ChatEvent("delta", {"text": "안"})
        yield chat_mod.ChatEvent("done", {"changed": True})

    monkeypatch.setattr("qatc.app.server.stream_turn", fake_stream)
    r = app.test_client().post("/api/chat", json={"message": "x", "content": None})
    assert r.status_code == 200
    assert r.mimetype == "text/event-stream"
    text = r.get_data(as_text=True)
    assert "event: delta" in text
    assert "event: done" in text


def test_chat_auth_error_reaches_the_browser(app, monkeypatch):
    from qatc.app import chat as chat_mod

    def fake_stream(cfg, message, content, **kw):
        yield chat_mod.ChatEvent("error", {"kind": "auth", "message": "재인증이 필요합니다."})

    monkeypatch.setattr("qatc.app.server.stream_turn", fake_stream)
    text = app.test_client().post("/api/chat", json={"message": "x"}).get_data(as_text=True)
    assert "event: error" in text
    assert "재인증" in text


def test_the_backend_never_writes_to_the_knowledge_db(app):
    """앱이 게이트를 우회할 수 없다는 것이 이 설계의 중심 성질이다.

    `qatc/app/` 안에서 쓰기 메서드를 부르면 채팅→CLI→게이트 경로 밖에
    두 번째 쓰기 경로가 생긴다. 선행 브랜치에서 이 불변식이 세 번 다시
    뚫렸고, 매번 "닫았다"고 판단한 다음 라운드에서 열렸다.
    """
    import pathlib

    src = "\n".join(
        p.read_text(encoding="utf-8")
        for p in pathlib.Path("qatc/app").rglob("*.py")
    )
    for writer in ("add_testcase", "set_slot", "init_content",
                   "replace_generated", "add_slot", "update_testcase_row"):
        assert writer not in src, f"앱이 쓰기 메서드를 부른다: {writer}"
```

- [ ] **Step 3: 실패 확인**

Run: `.venv\Scripts\python.exe -m pytest tests/test_app_server.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'qatc.app.server'`

- [ ] **Step 4: 구현**

`create_app(cfg)` 가 라우트를 등록한다. SSE 는 `Response(generate(), mimetype="text/event-stream")` 로 내보내고 각 이벤트를 `event: <kind>\ndata: <json>\n\n` 으로 쓴다. `ContentNotFound` 는 404 + `{"error": str(exc)}`, `ClaudeMissing` 은 `/api/health` 에서 `"missing"` 으로 보고한다.

`/api/export` 는 `qatc/export/tc_excel.py` 의 `export_tc_excel` 을 직접 부르고 `os.startfile()` 로 Excel 을 띄운다. `ExportBlocked` 는 409 + 그 예외의 한국어 문장을 그대로 쓴다 (`str(exc)`).

- [ ] **Step 5: 통과 확인**

Run: `.venv\Scripts\python.exe -m pytest`
Expected: 444 + 8 = 452 passed

- [ ] **Step 6: 뮤테이션 검증**

| # | 뮤테이션 | 죽어야 하는 테스트 |
|---|---|---|
| M17 | `ContentNotFound` 를 잡지 않고 500 으로 | `test_missing_content_is_a_korean_error_not_a_traceback` |
| M18 | 오류 본문에 `type(exc).__name__` 포함 | 위와 동일 |
| M19 | SSE mimetype 을 `application/json` 으로 | `test_chat_streams_server_sent_events` |
| M20 | `error` 이벤트를 프런트로 안 보냄 | `test_chat_auth_error_reaches_the_browser` |
| M21 | `views.py` 에 `st.set_slot(...)` 한 줄 추가 | `test_the_backend_never_writes_to_the_knowledge_db` |

- [ ] **Step 7: 커밋**

```bash
git add qatc/app/server.py tests/test_app_server.py pyproject.toml
git commit -m "서버와 라우트 — SSE 채팅, 읽기 API, 엑셀 열기"
```

---

### Task 4: 프런트엔드 3분할

**Files:**
- Create: `qatc/app/static/index.html`, `app.css`, `app.js`
- Modify: `tests/test_app_server.py`

**Interfaces:** 없음 (정적 파일). `app.js` 는 Task 3 의 API 모양만 안다.

**만들 것:** 왼쪽 트리(게임 > 컨텐츠 > 계열 > TC), 가운데 채팅(입력 + 스트리밍 출력 + 도구 호출 표시), 오른쪽 TC 검토(선택한 TC 의 절차·기대결과·근거 슬롯·`근거 철회됨`·`사람이 고침` 배지, 그리고 [엑셀로 열기] 버튼).

**만들지 않을 것:** 인증 · 다중 사용자 · TC 직접 편집 · 슬롯 편집 · 검색 · 필터 · 테마.

**갱신:** 채팅 턴이 `done` 으로 끝나면 `/api/tree` 와 `/api/content` 를 다시 가져온다. `db_mtime` 이 이전과 같으면 렌더를 건너뛴다. 폴링도 파일 감시도 쓰지 않는다 — 쓰기가 채팅으로만 일어나므로 턴 종료가 바뀌었을 수 있는 유일한 시점이다.

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_app_server.py` 에 추가한다:

```python
def test_index_has_the_three_panes(app):
    html = app.test_client().get("/").get_data(as_text=True)
    for pane in ("tree", "chat", "review"):
        assert f'id="{pane}"' in html, f"{pane} 패널이 없습니다"


def test_static_assets_are_served(app):
    c = app.test_client()
    for path in ("/static/app.css", "/static/app.js"):
        assert c.get(path).status_code == 200, path


def test_the_page_loads_nothing_from_the_network(app):
    """로컬 앱이다. 외부 CDN 을 물면 오프라인에서 죽는다."""
    html = app.test_client().get("/").get_data(as_text=True)
    assert "http://" not in html
    assert "https://" not in html
```

- [ ] **Step 2: 실패 확인 → 구현 → 통과 확인**

Run: `.venv\Scripts\python.exe -m pytest`
Expected: 452 + 3 = 455 passed

- [ ] **Step 3: 뮤테이션 검증**

| # | 뮤테이션 | 죽어야 하는 테스트 |
|---|---|---|
| M22 | `id="review"` 를 지움 | `test_index_has_the_three_panes` |
| M23 | `index.html` 에 CDN `<script src="https://...">` 추가 | `test_the_page_loads_nothing_from_the_network` |

- [ ] **Step 4: 커밋**

```bash
git add qatc/app/static tests/test_app_server.py
git commit -m "3분할 화면 — 트리·채팅·TC 검토"
```

---

### Task 5: `qatc app` 명령과 문서

**Files:**
- Modify: `qatc/cli.py`
- Modify: `README.md`
- Modify: `.claude/settings.json`
- Modify: `tests/test_app_server.py`, `tests/test_interview_skill.py`

- [ ] **Step 1: 실패하는 테스트 작성**

```python
def test_app_command_is_registered():
    from qatc.cli import build_parser
    assert "app" in build_parser()._subparsers._group_actions[0].choices


def test_readme_documents_the_app_command():
    from pathlib import Path
    text = (Path(__file__).resolve().parent.parent / "README.md").read_text(encoding="utf-8")
    assert "qatc app" in text
    assert "브라우저" in text
```

- [ ] **Step 2: `qatc app` 구현**

`cmd_app(args, cfg)` 가 `qatc.app.server.run(cfg, port=args.port)` 를 부르고 브라우저를 연다 (`webbrowser.open`). 포트가 이미 쓰이면 다음 포트를 시도하고 **실제 주소를 `_p()` 로 알린다.**

파서에 `ap = sub.add_parser("app", help="3분할 앱을 띄운다")` 와 `ap.add_argument("--port", type=int, default=8765)` 를 넣는다.

- [ ] **Step 3: `README.md` 갱신**

"지금 동작하는 명령" 목록에 `qatc app` 을 넣고, 앱이 무엇을 하는지 두세 줄로 적는다. **`--json` 규칙처럼 실제 출력을 실행해서 확인하고 인용할 것.**

- [ ] **Step 4: 통과 확인 → 뮤테이션 → 커밋**

Run: `.venv\Scripts\python.exe -m pytest`
Expected: 455 + 2 = 457 passed

```bash
git add qatc/cli.py README.md .claude/settings.json tests/
git commit -m "qatc app 명령과 문서"
```

---

### Task 6: 수동 확인 (사용자와 함께)

**이 태스크는 코드를 쓰지 않는다.** 구현자는 아래를 실행하고 관찰한 것을 보고서에 적는다.

- [ ] **Step 1: `claude` 인증 확인**

```bash
claude -p "Reply with exactly: OK" --output-format json --model sonnet
```

`is_error: true` 면 **여기서 멈추고 보고한다** — 사용자가 로그인해야 한다.

- [ ] **Step 2: 앱을 띄우고 실제로 한 컨텐츠를 인터뷰한다**

```bash
.venv\Scripts\qatc.exe app
```

관찰하고 보고할 것:
- 권한 승인 창이 뜨는가 (허용목록 구멍)
- 질문이 자연스러운가, 슬롯 이름을 그대로 읊는가
- 개괄 답변에서 `--types` 를 제대로 뽑는가
- 답을 정정했을 때 `근거 철회됨` 표시가 실제로 도움이 되는가
- 왼쪽·오른쪽이 턴 종료 후 갱신되는가
- [엑셀로 열기] 가 진짜 Excel 을 띄우는가
- 토큰이 만료됐을 때 화면에 재인증 안내가 뜨는가 (`claude` 로그아웃 후 확인 가능하면)

**고칠 것이 나오면 고치지 말고 목록으로 보고한다.** 이 태스크의 산출물은 코드가 아니라 관찰이다.

---

## 완료 기준

- 전체 스위트 **457 passed**
- `qatc app` 으로 브라우저가 열리고 세 패널이 뜬다
- 채팅으로 슬롯을 채우면 왼쪽·오른쪽이 턴 종료 후 갱신된다
- [엑셀로 열기] 가 Excel 을 띄우고, 파일이 잠겨 있으면 한국어 안내가 뜬다
- `qatc/app/` 안에 지식 DB 쓰기 메서드 호출이 0
- `git status --porcelain` 비어 있음
- 뮤테이션 23종 전부 검출
